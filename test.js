let arr = [1, 2, 3];

function func(el) {
    return el;
}

function filter(arr, func) {
  let newItem;
  for (let i = 0; i < arr.legth; i++) {
    if (func(arr[i])) {
      return newArr.push(newItem);
    } else return;
  }
}
